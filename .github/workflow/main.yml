name: Gatsby Publish

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: enriikke/gatsby-gh-pages-action@v2.1.1
        with:
          access-token: ${{ secrets.amap }}
          deploy-branch: gh-pages
          gatsby-args: --prefix-paths
<<<<<<< HEAD:.github/workflow/main.yml
=======

>>>>>>> 643c2f12ec5ab023112757fc5627ab6e484a77c0:.github/workflows/mail.yml
